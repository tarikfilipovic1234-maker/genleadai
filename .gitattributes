# Normalize line endings so the repository stays consistent across the
# Windows frontend work and the Linux containers the backend deploys into.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.svg text eol=lf
